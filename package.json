{
  "name": "landing-page",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "dependencies": {},
  "devDependencies": {
    "purgecss": "^1.1.0",
    "tailwindcss": "^0.7.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-css": "./node_modules/.bin/tailwind build styles.css -c ./tailwind.js -o ./output.css --no-autoprefixer",
    "purgecss": "./node_modules/.bin/purgecss --out build/css --config ./purgecss.config.js",
    "build-css": "npm run generate-css && npm run purgecss"
  },
  "author": "Evan Hackett",
  "license": "ISC"
}
